---
/**
 * 프로젝트 루트 **안**에 있는 forwarding 래퍼. 루트 밖에 있는
 * `shared/ForwardedButton.astro` 와 달리 이 파일도 주입 대상이라, 호출부 좌표와
 * 래퍼 자신의 좌표가 같은 `<button>` 에 함께 실린다.
 */
const props = Astro.props;
---

<button type="button" {...props}>
  <slot />
</button>
